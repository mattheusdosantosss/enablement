Coloque aqui os arquivos de fonte do projeto.

Formatos suportados: .woff2 (preferido), .woff, .ttf, .otf

Após inserir o arquivo, atualize app/layout.tsx com o nome correto do arquivo.
O projeto já está configurado para carregar a fonte daqui via next/font/local.

Exemplo esperado: Archivo.woff2 (ou o arquivo da fonte PSA)
